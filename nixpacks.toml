[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview"

[variables]
NIXPACKS_PROJECT_TYPE = "node"
NODE_ENV = "production"
